#include <iostream.h>

int main(v)
{
   int  i, n;

   cout << "Veuillez entrer un nombre : ";
   cin >> n;

   int *tableau = new int [ n ];

   for(i=0 ; i<n ; i++)
      cin >> tableau [ i ];
   for(i=0 ; i<n ; i++)
      cout << tableau [ i ];

   delete [] tableau;

   return 0;
}
