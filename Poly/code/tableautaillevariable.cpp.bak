#include <iostream.h>


int main()
{
   int n, i;
   int *someArray = NULL;

   cout << "Veuillez entrer un nombre : ";
   cin >> n;

   tableau = new int [ n ];

   for(i=0 ; i<n ; i++)
      cin >> tableau [ i ];
   for(i=0 ; i<n ; i++)
      cout << tableau [ i ];

   /*attention, il manque quelque chose*/
   return 0;
}
